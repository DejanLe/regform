  <% if user_signed_in? %>
<div class="new_requestor">
  <div class="container">
    <div class="row">
      <div class="col-md-12 mx-auto">  
				 	<%= render  @client.requests %>
				  <%= render "requests/form_request" %> 
				 
	    	</div>
			  <hr>  
         <%= render @client.personnel_infos %> 
 	     </div> 
    </div>
  </div>
 
  <% end %>